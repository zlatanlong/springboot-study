spring:
  profiles:
    active: dev
logging:
  level:
    cn.lcl.mapper: trace
  pattern:
#    console: '%p%m%n'
mybatis-plus:
  mapper-locations:
    - classpath*:mapper/*.xml
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0